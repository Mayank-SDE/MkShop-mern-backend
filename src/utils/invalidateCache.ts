import { InvalidateCache } from '../types/productType.js';

export const invalidateCache = ({ product, order, admin }: InvalidateCache) => {
  if (product) {
  }
  if (order) {
  }
  if (admin) {
  }
};
